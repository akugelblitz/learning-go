package main

import "fmt"

func main() {
	// prices := [4]string{"hi", "there", "are", "you"}
	// slice := prices[1:3]
	// slice[0] = "funk"
	// fmt.Println(slice)
	// fmt.Println(prices)
	// fmt.Println(len(prices), cap(prices), len(slice), cap(slice))
	// append(sli``, "whh")
	//
	// price := [2]float64{10.99, 8.99}
	// fmt.Println(price)
	// prices := price[0:]
	// slice := append(prices, 5.99)
	// fmt.Println(slice)
	// slice[0] = 2
	// fmt.Println(prices)
	// fmt.Println(slice, cap(slice))
	// slice2 := append(slice, 4.1)
	// fmt.Println(slice2)
	// slice[2] = 3
	// fmt.Println(prices)
	// fmt.Println(slice, cap(slice))
	// fmt.Println(slice2)
	// final := append(slice, slice2...)
	// fmt.Println(final)
	//
	// websites := map[string]string{
	// 	"google.com": "https://google.com", "aws": "https://aws.com",
	// }
	// fmt.Println(websites)
	// websites["kin"] = "kino"
	// delete(websites, "google.com")
	// fmt.Println(websites)
	// prices := make([]string, 0, 10)
	// fmt.Println(prices)
}
